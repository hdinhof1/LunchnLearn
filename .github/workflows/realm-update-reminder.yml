name: realm-update-reminder
on:
  push: ## should be on pull request
    branches:
      - master
      - staging
    paths:
      - 'learning.swift'

jobs:
  remind:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: run a command here
    run: echo Hello, realm!